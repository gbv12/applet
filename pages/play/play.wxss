/* pages/play/play.wxss */
.box{
  text-align: center;
}

image{
  width: 300rpx;
  border-radius: 50%;

  animation: abc 5s linear infinite;
}

@keyframes abc{
  0%{
    transform: rotate(0deg);
  }
  100%{
    transform: rotate(360deg);
  }
}